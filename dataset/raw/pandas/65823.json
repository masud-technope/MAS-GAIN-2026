{
  "repo": "pandas-dev/pandas",
  "pull_request_id": 65823,
  "title": "BUG: Fix pd.array with NumPy masked arrays",
  "description": "Ref: https://github.com/pandas-dev/pandas/pull/63887/changes#r3350391771\r\n\r\nWhen looking at this, found a lot more deficiencies in the implementation. Rewrote logic and added tests.",
  "changed_files": [
    {
      "path": "doc/source/whatsnew/v3.1.0.rst",
      "status": "modified",
      "additions": 1,
      "deletions": 1,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-1",
      "patch": "@@ -287,7 +287,7 @@ Conversion\n - Bug in :class:`DataFrame` constructor where ``NaT`` in a :class:`TimedeltaIndex` row was incorrectly inferred as ``datetime64`` instead of ``timedelta64`` (:issue:`23985`)\n - Bug in :class:`DataFrame` constructor where constructing from a list of uniform-dtype arrays (e.g. pyarrow, :class:`CategoricalDtype`, nullable dtypes) lost the dtype (:issue:`49593`)\n - Bug in :func:`pd.array` silently converting NaN to a nonsensical integer when given float data containing NaN and a NumPy integer dtype (:issue:`41724`)\n-- Fixed :func:`pandas.array` to preserve mask information when converting NumPy masked arrays, converting masked values to missing values (:issue:`63879`).\n+- Fixed :func:`pandas.array` to preserve mask information when converting NumPy masked arrays, converting masked values to missing values (:issue:`63879`)\n - Fixed bug in :meth:`DataFrame.from_records` where ``exclude`` was ignored when ``data`` was an iterator and ``nrows=0`` (:issue:`63774`)\n \n Strings",
      "changed_code": "- Bug in :class:`DataFrame` constructor where ``NaT`` in a :class:`TimedeltaIndex` row was incorrectly inferred as ``datetime64`` instead of ``timedelta64`` (:issue:`23985`)\n- Bug in :class:`DataFrame` constructor where constructing from a list of uniform-dtype arrays (e.g. pyarrow, :class:`CategoricalDtype`, nullable dtypes) lost the dtype (:issue:`49593`)\n- Bug in :func:`pd.array` silently converting NaN to a nonsensical integer when given float data containing NaN and a NumPy integer dtype (:issue:`41724`)\n- Fixed :func:`pandas.array` to preserve mask information when converting NumPy masked arrays, converting masked values to missing values (:issue:`63879`).\n- Fixed :func:`pandas.array` to preserve mask information when converting NumPy masked arrays, converting masked values to missing values (:issue:`63879`)\n- Fixed bug in :meth:`DataFrame.from_records` where ``exclude`` was ignored when ``data`` was an iterator and ``nrows=0`` (:issue:`63774`)\n\nStrings",
      "review_comments": [
        {
          "id": 4712558436,
          "user": "rhshadrach",
          "body": "@jorisvandenbossche - tests have been added.",
          "line": null,
          "original_line": null,
          "position": null,
          "target_code": "",
          "source": "issue_comment"
        },
        {
          "id": 4715818617,
          "user": "jorisvandenbossche",
          "body": "Thanks Richard!",
          "line": null,
          "original_line": null,
          "position": null,
          "target_code": "",
          "source": "issue_comment"
        }
      ]
    },
    {
      "path": "pandas/core/construction.py",
      "status": "modified",
      "additions": 32,
      "deletions": 10,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "patch": "@@ -29,7 +29,6 @@\n from pandas.core.dtypes.cast import (\n     construct_1d_arraylike_from_scalar,\n     construct_1d_object_array_from_listlike,\n-    ensure_dtype_can_hold_na,\n     maybe_cast_to_datetime,\n     maybe_cast_to_integer_array,\n     maybe_promote,\n@@ -40,14 +39,20 @@\n     is_object_dtype,\n     pandas_dtype,\n )\n-from pandas.core.dtypes.dtypes import NumpyEADtype\n+from pandas.core.dtypes.dtypes import (\n+    BaseMaskedDtype,\n+    NumpyEADtype,\n+)\n from pandas.core.dtypes.generic import (\n     ABCDataFrame,\n     ABCExtensionArray,\n     ABCIndex,\n     ABCSeries,\n )\n-from pandas.core.dtypes.missing import isna\n+from pandas.core.dtypes.missing import (\n+    isna,\n+    na_value_for_dtype,\n+)\n \n import pandas.core.common as com\n \n@@ -327,14 +332,31 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()\n+            except (KeyError, NotImplementedError):\n+                # No nullable masked dtype for this kind.\n+                if data.dtype.char in \"SU\":\n+                    data = lib.ensure_string_array(np.asarray(data.data))\n+                    data[mask] = np.nan\n+                else:\n+                    # datetime64/timedelta64 use NaT; float16/complex/object NaN.\n+                    # Argument to \"filled\" of \"MaskedArray\" has incompatible type...;\n+                    # expected \"complex | str | bytes | generic[Any] | None\"\n+                    data = data.filled(\n+                        na_value_for_dtype(data.dtype, compat=False)  # type: ignore[arg-type]\n+                    )\n+            else:\n+                # Argument 2 to \"BaseMaskedArray\" has incompatible type...;\n+                # expected \"ndarray[tuple[Any, ...], dtype[numpy.bool[builtins.bool]]]\"\n+                data = masked_cls(np.asarray(data.data), mask, copy=copy)  # type: ignore[arg-type]\n \n     # this returns None for not-found dtypes.\n     if dtype is not None:",
      "changed_code": "from pandas.core.dtypes.cast import (\n    construct_1d_arraylike_from_scalar,\n    construct_1d_object_array_from_listlike,\n    ensure_dtype_can_hold_na,\n    maybe_cast_to_datetime,\n    maybe_cast_to_integer_array,\n    maybe_promote,\n    is_object_dtype,\n    pandas_dtype,\n)\nfrom pandas.core.dtypes.dtypes import NumpyEADtype\nfrom pandas.core.dtypes.dtypes import (\n    BaseMaskedDtype,\n    NumpyEADtype,\n)\nfrom pandas.core.dtypes.generic import (\n    ABCDataFrame,\n    ABCExtensionArray,\n    ABCIndex,\n    ABCSeries,\n)\nfrom pandas.core.dtypes.missing import isna\nfrom pandas.core.dtypes.missing import (\n    isna,\n    na_value_for_dtype,\n)\n\nimport pandas.core.common as com\n\n                \"Cannot construct an array from an ndarray with compound dtype. \"\n                \"Use DataFrame instead.\"\n            )\n        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n            if na_dtype.char in \"SU\":\n                na_dtype = np.dtype(\"object\")\n            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n        else:\n            # No mask, convert to regular array\n        mask = ma.getmask(data)\n        if mask is ma.nomask:\n            # Avoid materializing an all-False mask.\n            data = np.asarray(data)\n        else:\n            try:\n                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n                    data.dtype\n                ).construct_array_type()\n            except (KeyError, NotImplementedError):\n                # No nullable masked dtype for this kind.\n                if data.dtype.char in \"SU\":\n                    data = lib.ensure_string_array(np.asarray(data.data))\n                    data[mask] = np.nan\n                else:\n                    # datetime64/timedelta64 use NaT; float16/complex/object NaN.\n                    # Argument to \"filled\" of \"MaskedArray\" has incompatible type...;\n                    # expected \"complex | str | bytes | generic[Any] | None\"\n                    data = data.filled(\n                        na_value_for_dtype(data.dtype, compat=False)  # type: ignore[arg-type]\n                    )\n            else:\n                # Argument 2 to \"BaseMaskedArray\" has incompatible type...;\n                # expected \"ndarray[tuple[Any, ...], dtype[numpy.bool[builtins.bool]]]\"\n                data = masked_cls(np.asarray(data.data), mask, copy=copy)  # type: ignore[arg-type]\n\n    # this returns None for not-found dtypes.\n    if dtype is not None:",
      "review_comments": [
        {
          "id": 3371693509,
          "user": "jorisvandenbossche",
          "body": "Should this first check the specified `dtype` if not None, and only otherwise infer it from the `data.dtype`?\n\n(for the case a user does eg `pd.array(masked_array_float32, dtype=\"Float64\")`)",
          "line": 343,
          "original_line": 343,
          "position": 50,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()",
          "source": "review_comment"
        },
        {
          "id": 3371718853,
          "user": "jorisvandenbossche",
          "body": "I was going to comment that this should probably be `pd.NA` instead of `np.nan`, since `pd.array()` defaults to nullable dtypes, to avoid another conversion from NaN to NA later on. But I see that the `_from_sequence` will check and replace regardless of NaN or NA, so it does not really matter that much (and in case of pyarrow strings, it will get converted to a pyarrow array anyway, and in that case it also does not matter)",
          "line": 348,
          "original_line": 348,
          "position": 55,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()\n+            except (KeyError, NotImplementedError):\n+                # No nullable masked dtype for this kind.\n+                if data.dtype.char in \"SU\":\n+                    data = lib.ensure_string_array(np.asarray(data.data))\n+                    data[mask] = np.nan",
          "source": "review_comment"
        },
        {
          "id": 3376037534,
          "user": "rhshadrach",
          "body": "```python\r\nmasked_array_float32 = np.ma.masked_array([1.0], dtype=\"float32\")\r\nprint(pd.array(masked_array_float32, dtype=\"Float64\"))\r\n# <FloatingArray>\r\n# [1.0]\r\n# Length: 1, dtype: Float64\r\n```\r\n\r\nThis block just converts the masked array into its corresponding type, the dtype is then converted downstream. Now this will in some cases do an extra copy, but I believe the logic would be more complex if we were to try to avoid that.",
          "line": 343,
          "original_line": 343,
          "position": 50,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()",
          "source": "review_comment"
        },
        {
          "id": 3386673182,
          "user": "jorisvandenbossche",
          "body": "Ah, I was assuming the constructed masked array would be returned directly, but there is indeed no `return`. Ignore!",
          "line": 343,
          "original_line": 343,
          "position": 50,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()",
          "source": "review_comment"
        },
        {
          "id": 3386679535,
          "user": "jorisvandenbossche",
          "body": "(but, that might not yet be explicitly covered by the tests?)",
          "line": 343,
          "original_line": 343,
          "position": 50,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()",
          "source": "review_comment"
        },
        {
          "id": 3399405684,
          "user": "rhshadrach",
          "body": "Yea - good call. Will add.",
          "line": 343,
          "original_line": 343,
          "position": 50,
          "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()",
          "source": "review_comment"
        }
      ]
    },
    {
      "path": "pandas/tests/arrays/test_array.py",
      "status": "modified",
      "additions": 114,
      "deletions": 24,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-3",
      "patch": "@@ -8,6 +8,8 @@\n \n from pandas._config import using_string_dtype\n \n+import pandas.util._test_decorators as td\n+\n import pandas as pd\n import pandas._testing as tm\n from pandas.api.extensions import register_extension_dtype\n@@ -17,12 +19,10 @@\n     FloatingArray,\n     IntegerArray,\n     IntervalArray,\n+    NumpyExtensionArray,\n     SparseArray,\n     TimedeltaArray,\n )\n-from pandas.core.arrays import (\n-    NumpyExtensionArray,\n-)\n from pandas.tests.extension.decimal import (\n     DecimalArray,\n     DecimalDtype,\n@@ -577,37 +577,127 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_has_mask = np_dtype not in [\n+        \"float16\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ]\n+    if result_has_mask:\n+        result_data = result._data\n+    else:\n+        result_data = result._ndarray\n+\n+    expect_data_shares = not (\n+        copy\n+        # np.shares_memory is always False on length 0.\n+        or len(ma_arr) == 0\n+        # If the result has no mask, data needs to be modified if there are NA values\n+        or (np.any(mask) and not result_has_mask)\n+    )\n+    assert np.shares_memory(result_data, ma_arr.data) == expect_data_shares\n+\n+    if not result_has_mask:\n+        return\n \n-def test_pd_array_from_masked_array_preserves_mask_string():\n-    # GH#63879\n-    # String masked array should produce StringArray with pd.NA where mask=True\n-    ma_arr = ma.array([\"a\", \"b\", \"c\", \"d\"], mask=[False, True, False, True])\n-    result = pd.array(ma_arr)\n-    expected = pd.array([\"a\", pd.NA, \"c\", pd.NA], dtype=\"string\")\n+    expect_mask_shares = not (copy or len(ma_arr) == 0 or mask is np.False_)\n+    assert np.shares_memory(result._mask, ma_arr.mask) == expect_mask_shares\n+\n+\n+@pytest.mark.parametrize(\"dtype\", [None, \"Int64\"])\n+def test_masked_array_no_float_detour(dtype):\n+    # GH#63879 - ensure we don't round-trip through float64\n+    ma_arr = ma.array([2**53 + 1, 2, 3], mask=[False, True, False], dtype=np.int64)\n+    result = pd.array(ma_arr, dtype=dtype)\n+    expected = pd.array([2**53 + 1, pd.NA, 3], dtype=\"Int64\")\n     tm.assert_extension_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_no_mask():\n-    # GH#63879 - edge case: mask is all False\n-    ma_arr = ma.array([1, 2, 3], mask=[False, False, False], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\n+    \"np_dtype, dtype\",\n+    [\n+        (\"float32\", \"Float64\"),\n+        (\"int64\", \"Float64\"),\n+        (\"int64\", \"Int32\"),\n+        (\"float64\", \"Int64\"),\n+        (\"bool\", \"boolean\"),\n+        (\"float64\", \"string\"),\n+        (\"datetime64[ns]\", \"datetime64[us]\"),\n+        (\"timedelta64[ns]\", \"timedelta64[us]\"),\n+        pytest.param(\"int64\", \"int64[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n+        pytest.param(\"float64\", \"float64[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n+        pytest.param(\"bool\", \"bool[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n+        pytest.param(\"float64\", \"string[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n+        pytest.param(\n+            \"datetime64[ns]\",\n+            \"timestamp[ns][pyarrow]\",\n+            marks=td.skip_if_no(\"pyarrow\"),\n+        ),\n+        pytest.param(\n+            \"timedelta64[ns]\",\n+            \"duration[ns][pyarrow]\",\n+            marks=td.skip_if_no(\"pyarrow\"),\n+        ),\n+    ],\n+)\n+def test_masked_array_explicit_dtype(np_dtype, dtype):\n+    # GH#63879 - an explicitly passed dtype takes precedence over the dtype\n+    # inferred from the masked array, and masked values become NA\n+    ma_arr = ma.array([1, 2, 3], mask=[False, True, False], dtype=np_dtype)\n+    result = pd.array(ma_arr, dtype=dtype)\n+    expected = pd.array(ma_arr).astype(dtype)\n     tm.assert_extension_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_nomask():\n-    # GH#63879 - edge case: mask is nomask\n-    ma_arr = ma.array([1, 2, 3], mask=ma.nomask, dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\n+    \"data, expected\",\n+    [\n+        ([\"a\", \"b\", \"c\"], [\"a\", pd.NA, \"c\"]),\n+        ([b\"abc\", b\"def\", \"caf\u00e9\".encode()], [\"abc\", pd.NA, \"caf\u00e9\"]),\n+    ],\n+    ids=[\"str\", \"bytes\"],\n+)\n+def test_masked_array_string(data, expected):\n+    # GH#63879 - str and bytes masked arrays both infer the string dtype (bytes\n+    # are UTF-8 decoded), consistently with the no-missing-value case\n+    result = pd.array(ma.array(data, mask=[False, True, False]))\n+    expected = pd.array(expected, dtype=\"string\")\n     tm.assert_extension_array_equal(result, expected)\n \n ",
      "changed_code": "from pandas._config import using_string_dtype\n\nimport pandas.util._test_decorators as td\n\nimport pandas as pd\nimport pandas._testing as tm\nfrom pandas.api.extensions import register_extension_dtype\n    FloatingArray,\n    IntegerArray,\n    IntervalArray,\n    NumpyExtensionArray,\n    SparseArray,\n    TimedeltaArray,\n)\nfrom pandas.core.arrays import (\n    NumpyExtensionArray,\n)\nfrom pandas.tests.extension.decimal import (\n    DecimalArray,\n    DecimalDtype,\n    tm.assert_numpy_array_equal(result, expected)\n\n\ndef test_pd_array_from_masked_array_preserves_mask_integer():\n@pytest.mark.parametrize(\n    \"data, mask\",\n    [\n        ([1, 2, 3], [False, True, False]),\n        ([1, 2, 3], [False, False, False]),\n        ([1, 2, 3], ma.nomask),\n        ([], ma.nomask),\n    ],\n)\n@pytest.mark.parametrize(\n    \"np_dtype\",\n    [\n        \"uint8\",\n        \"int32\",\n        \"int64\",\n        \"bool\",\n        \"float16\",\n        \"float32\",\n        \"complex128\",\n        \"datetime64[ns]\",\n        \"timedelta64[ns]\",\n    ],\n)\n@pytest.mark.parametrize(\"copy\", [True, False])\ndef test_masked_array_kinds(data, mask, np_dtype, copy):\n    # GH#63879\n    # Integer masked array should produce Int64 with pd.NA where mask=True\n    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n    result = pd.array(ma_arr)\n    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n    arr = np.array(data, dtype=np_dtype)\n    ma_arr = ma.array(arr, mask=mask)\n    result = pd.array(ma_arr, copy=copy)\n    expected = pd.array(arr)\n    if np.any(mask):\n        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n        expected[np.asarray(mask)] = na\n    tm.assert_extension_array_equal(result, expected)\n\n    # Check whether we made a copy.\n    result_has_mask = np_dtype not in [\n        \"float16\",\n        \"complex128\",\n        \"datetime64[ns]\",\n        \"timedelta64[ns]\",\n    ]\n    if result_has_mask:\n        result_data = result._data\n    else:\n        result_data = result._ndarray\n\n    expect_data_shares = not (\n        copy\n        # np.shares_memory is always False on length 0.\n        or len(ma_arr) == 0\n        # If the result has no mask, data needs to be modified if there are NA values\n        or (np.any(mask) and not result_has_mask)\n    )\n    assert np.shares_memory(result_data, ma_arr.data) == expect_data_shares\n\n    if not result_has_mask:\n        return\n\ndef test_pd_array_from_masked_array_preserves_mask_string():\n    # GH#63879\n    # String masked array should produce StringArray with pd.NA where mask=True\n    ma_arr = ma.array([\"a\", \"b\", \"c\", \"d\"], mask=[False, True, False, True])\n    result = pd.array(ma_arr)\n    expected = pd.array([\"a\", pd.NA, \"c\", pd.NA], dtype=\"string\")\n    expect_mask_shares = not (copy or len(ma_arr) == 0 or mask is np.False_)\n    assert np.shares_memory(result._mask, ma_arr.mask) == expect_mask_shares\n\n\n@pytest.mark.parametrize(\"dtype\", [None, \"Int64\"])\ndef test_masked_array_no_float_detour(dtype):\n    # GH#63879 - ensure we don't round-trip through float64\n    ma_arr = ma.array([2**53 + 1, 2, 3], mask=[False, True, False], dtype=np.int64)\n    result = pd.array(ma_arr, dtype=dtype)\n    expected = pd.array([2**53 + 1, pd.NA, 3], dtype=\"Int64\")\n    tm.assert_extension_array_equal(result, expected)\n\n\ndef test_pd_array_from_masked_array_no_mask():\n    # GH#63879 - edge case: mask is all False\n    ma_arr = ma.array([1, 2, 3], mask=[False, False, False], dtype=np.int64)\n    result = pd.array(ma_arr)\n    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n@pytest.mark.parametrize(\n    \"np_dtype, dtype\",\n    [\n        (\"float32\", \"Float64\"),\n        (\"int64\", \"Float64\"),\n        (\"int64\", \"Int32\"),\n        (\"float64\", \"Int64\"),\n        (\"bool\", \"boolean\"),\n        (\"float64\", \"string\"),\n        (\"datetime64[ns]\", \"datetime64[us]\"),\n        (\"timedelta64[ns]\", \"timedelta64[us]\"),\n        pytest.param(\"int64\", \"int64[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n        pytest.param(\"float64\", \"float64[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n        pytest.param(\"bool\", \"bool[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n        pytest.param(\"float64\", \"string[pyarrow]\", marks=td.skip_if_no(\"pyarrow\")),\n        pytest.param(\n            \"datetime64[ns]\",\n            \"timestamp[ns][pyarrow]\",\n            marks=td.skip_if_no(\"pyarrow\"),\n        ),\n        pytest.param(\n            \"timedelta64[ns]\",\n            \"duration[ns][pyarrow]\",\n            marks=td.skip_if_no(\"pyarrow\"),\n        ),\n    ],\n)\ndef test_masked_array_explicit_dtype(np_dtype, dtype):\n    # GH#63879 - an explicitly passed dtype takes precedence over the dtype\n    # inferred from the masked array, and masked values become NA\n    ma_arr = ma.array([1, 2, 3], mask=[False, True, False], dtype=np_dtype)\n    result = pd.array(ma_arr, dtype=dtype)\n    expected = pd.array(ma_arr).astype(dtype)\n    tm.assert_extension_array_equal(result, expected)\n\n\ndef test_pd_array_from_masked_array_nomask():\n    # GH#63879 - edge case: mask is nomask\n    ma_arr = ma.array([1, 2, 3], mask=ma.nomask, dtype=np.int64)\n    result = pd.array(ma_arr)\n    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n@pytest.mark.parametrize(\n    \"data, expected\",\n    [\n        ([\"a\", \"b\", \"c\"], [\"a\", pd.NA, \"c\"]),\n        ([b\"abc\", b\"def\", \"caf\u00e9\".encode()], [\"abc\", pd.NA, \"caf\u00e9\"]),\n    ],\n    ids=[\"str\", \"bytes\"],\n)\ndef test_masked_array_string(data, expected):\n    # GH#63879 - str and bytes masked arrays both infer the string dtype (bytes\n    # are UTF-8 decoded), consistently with the no-missing-value case\n    result = pd.array(ma.array(data, mask=[False, True, False]))\n    expected = pd.array(expected, dtype=\"string\")\n    tm.assert_extension_array_equal(result, expected)",
      "review_comments": [
        {
          "id": 3371752832,
          "user": "jorisvandenbossche",
          "body": "The logic here is clear, but I was first confused about the name `result_missing_mask`, because you are checking here for the dtypes where the result does _not_ use a mask for the missing values",
          "line": null,
          "original_line": 614,
          "position": 1,
          "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in [",
          "source": "review_comment"
        },
        {
          "id": 3371775959,
          "user": "jorisvandenbossche",
          "body": "I was first surprised to see the byte values here, because I would not expect we convert that to strings. \n\nWe also do not do that if passing them directly:\n\n```\n>>> pd.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()])\n<NumpyExtensionArray>\n[b'abc', b'def', b'caf\\xc3\\xa9']\nLength: 3, dtype: object\n```\n\nbut apparently when this is converted to a `S` numpy array first, it indeed gets converted to string:\n\n```\n>>> pd.array(np.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()]))\n<ArrowStringArray>\n['abc', 'def', 'caf\u00e9']\nLength: 3, dtype: string\n```\n\nAnd unfortunately Series has even a different inconsistency:\n\n```python\n>>> pd.Series([b\"abc\", b\"def\", \"caf\u00e9\".encode()])\n0            b'abc'\n1            b'def'\n2    b'caf\\xc3\\xa9'\ndtype: object\n>>> \n>>> pd.Series(np.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()]))\n0            b'abc'\n1            b'def'\n2    b'caf\\xc3\\xa9'\ndtype: bytes40\n```\n\nAnyway, that is for another issue, since it is not related to the masked aspect of the input array.",
          "line": 692,
          "original_line": 654,
          "position": 160,
          "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in [\n+        \"float16\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ]\n+    if result_missing_mask:\n+        result_data = result._ndarray\n+    else:\n+        result_data = result._data\n+\n+    expect_data_shares = not (\n+        copy\n+        # np.shares_memory is always False on length 0.\n+        or len(ma_arr) == 0\n+        # If the result has no mask, data needs to be modified if there are NA values\n+        or (np.any(mask) and result_missing_mask)\n+    )\n+    assert np.shares_memory(result_data, ma_arr.data) == expect_data_shares\n \n-def test_pd_array_from_masked_array_preserves_mask_string():\n-    # GH#63879\n-    # String masked array should produce StringArray with pd.NA where mask=True\n-    ma_arr = ma.array([\"a\", \"b\", \"c\", \"d\"], mask=[False, True, False, True])\n-    result = pd.array(ma_arr)\n-    expected = pd.array([\"a\", pd.NA, \"c\", pd.NA], dtype=\"string\")\n-    tm.assert_extension_array_equal(result, expected)\n+    if result_missing_mask:\n+        return\n+\n+    expect_mask_shares = not (copy or len(ma_arr) == 0 or mask is np.False_)\n+    assert np.shares_memory(result._mask, ma_arr.mask) == expect_mask_shares\n \n \n-def test_pd_array_from_masked_array_no_mask():\n-    # GH#63879 - edge case: mask is all False\n-    ma_arr = ma.array([1, 2, 3], mask=[False, False, False], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\"dtype\", [None, \"Int64\"])\n+def test_masked_array_no_float_detour(dtype):\n+    # GH#63879 - ensure we don't round-trip through float64\n+    ma_arr = ma.array([2**53 + 1, 2, 3], mask=[False, True, False], dtype=np.int64)\n+    result = pd.array(ma_arr, dtype=dtype)\n+    expected = pd.array([2**53 + 1, pd.NA, 3], dtype=\"Int64\")\n     tm.assert_extension_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_nomask():\n-    # GH#63879 - edge case: mask is nomask\n-    ma_arr = ma.array([1, 2, 3], mask=ma.nomask, dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\n+    \"data, expected\",\n+    [\n+        ([\"a\", \"b\", \"c\"], [\"a\", pd.NA, \"c\"]),\n+        ([b\"abc\", b\"def\", \"caf\u00e9\".encode()], [\"abc\", pd.NA, \"caf\u00e9\"]),",
          "source": "review_comment"
        },
        {
          "id": 3376046486,
          "user": "rhshadrach",
          "body": "Renamed to `result_has_mask`. Is that more clear?",
          "line": null,
          "original_line": 614,
          "position": 1,
          "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in [",
          "source": "review_comment"
        },
        {
          "id": 3386685683,
          "user": "jorisvandenbossche",
          "body": "Perfect!",
          "line": null,
          "original_line": 614,
          "position": 1,
          "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in [",
          "source": "review_comment"
        }
      ]
    }
  ],
  "review_comments": [
    {
      "id": 3371693509,
      "user": "jorisvandenbossche",
      "body": "Should this first check the specified `dtype` if not None, and only otherwise infer it from the `data.dtype`?\n\n(for the case a user does eg `pd.array(masked_array_float32, dtype=\"Float64\")`)",
      "path": "pandas/core/construction.py",
      "line": 343,
      "original_line": 343,
      "position": 50,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()"
    },
    {
      "id": 3371718853,
      "user": "jorisvandenbossche",
      "body": "I was going to comment that this should probably be `pd.NA` instead of `np.nan`, since `pd.array()` defaults to nullable dtypes, to avoid another conversion from NaN to NA later on. But I see that the `_from_sequence` will check and replace regardless of NaN or NA, so it does not really matter that much (and in case of pyarrow strings, it will get converted to a pyarrow array anyway, and in that case it also does not matter)",
      "path": "pandas/core/construction.py",
      "line": 348,
      "original_line": 348,
      "position": 55,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()\n+            except (KeyError, NotImplementedError):\n+                # No nullable masked dtype for this kind.\n+                if data.dtype.char in \"SU\":\n+                    data = lib.ensure_string_array(np.asarray(data.data))\n+                    data[mask] = np.nan"
    },
    {
      "id": 3371752832,
      "user": "jorisvandenbossche",
      "body": "The logic here is clear, but I was first confused about the name `result_missing_mask`, because you are checking here for the dtypes where the result does _not_ use a mask for the missing values",
      "path": "pandas/tests/arrays/test_array.py",
      "line": null,
      "original_line": 614,
      "position": 1,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-3",
      "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in ["
    },
    {
      "id": 3371775959,
      "user": "jorisvandenbossche",
      "body": "I was first surprised to see the byte values here, because I would not expect we convert that to strings. \n\nWe also do not do that if passing them directly:\n\n```\n>>> pd.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()])\n<NumpyExtensionArray>\n[b'abc', b'def', b'caf\\xc3\\xa9']\nLength: 3, dtype: object\n```\n\nbut apparently when this is converted to a `S` numpy array first, it indeed gets converted to string:\n\n```\n>>> pd.array(np.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()]))\n<ArrowStringArray>\n['abc', 'def', 'caf\u00e9']\nLength: 3, dtype: string\n```\n\nAnd unfortunately Series has even a different inconsistency:\n\n```python\n>>> pd.Series([b\"abc\", b\"def\", \"caf\u00e9\".encode()])\n0            b'abc'\n1            b'def'\n2    b'caf\\xc3\\xa9'\ndtype: object\n>>> \n>>> pd.Series(np.array([b\"abc\", b\"def\", \"caf\u00e9\".encode()]))\n0            b'abc'\n1            b'def'\n2    b'caf\\xc3\\xa9'\ndtype: bytes40\n```\n\nAnyway, that is for another issue, since it is not related to the masked aspect of the input array.",
      "path": "pandas/tests/arrays/test_array.py",
      "line": 692,
      "original_line": 654,
      "position": 160,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-3",
      "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in [\n+        \"float16\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ]\n+    if result_missing_mask:\n+        result_data = result._ndarray\n+    else:\n+        result_data = result._data\n+\n+    expect_data_shares = not (\n+        copy\n+        # np.shares_memory is always False on length 0.\n+        or len(ma_arr) == 0\n+        # If the result has no mask, data needs to be modified if there are NA values\n+        or (np.any(mask) and result_missing_mask)\n+    )\n+    assert np.shares_memory(result_data, ma_arr.data) == expect_data_shares\n \n-def test_pd_array_from_masked_array_preserves_mask_string():\n-    # GH#63879\n-    # String masked array should produce StringArray with pd.NA where mask=True\n-    ma_arr = ma.array([\"a\", \"b\", \"c\", \"d\"], mask=[False, True, False, True])\n-    result = pd.array(ma_arr)\n-    expected = pd.array([\"a\", pd.NA, \"c\", pd.NA], dtype=\"string\")\n-    tm.assert_extension_array_equal(result, expected)\n+    if result_missing_mask:\n+        return\n+\n+    expect_mask_shares = not (copy or len(ma_arr) == 0 or mask is np.False_)\n+    assert np.shares_memory(result._mask, ma_arr.mask) == expect_mask_shares\n \n \n-def test_pd_array_from_masked_array_no_mask():\n-    # GH#63879 - edge case: mask is all False\n-    ma_arr = ma.array([1, 2, 3], mask=[False, False, False], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\"dtype\", [None, \"Int64\"])\n+def test_masked_array_no_float_detour(dtype):\n+    # GH#63879 - ensure we don't round-trip through float64\n+    ma_arr = ma.array([2**53 + 1, 2, 3], mask=[False, True, False], dtype=np.int64)\n+    result = pd.array(ma_arr, dtype=dtype)\n+    expected = pd.array([2**53 + 1, pd.NA, 3], dtype=\"Int64\")\n     tm.assert_extension_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_nomask():\n-    # GH#63879 - edge case: mask is nomask\n-    ma_arr = ma.array([1, 2, 3], mask=ma.nomask, dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, 2, 3], dtype=\"Int64\")\n+@pytest.mark.parametrize(\n+    \"data, expected\",\n+    [\n+        ([\"a\", \"b\", \"c\"], [\"a\", pd.NA, \"c\"]),\n+        ([b\"abc\", b\"def\", \"caf\u00e9\".encode()], [\"abc\", pd.NA, \"caf\u00e9\"]),"
    },
    {
      "id": 3376037534,
      "user": "rhshadrach",
      "body": "```python\r\nmasked_array_float32 = np.ma.masked_array([1.0], dtype=\"float32\")\r\nprint(pd.array(masked_array_float32, dtype=\"Float64\"))\r\n# <FloatingArray>\r\n# [1.0]\r\n# Length: 1, dtype: Float64\r\n```\r\n\r\nThis block just converts the masked array into its corresponding type, the dtype is then converted downstream. Now this will in some cases do an extra copy, but I believe the logic would be more complex if we were to try to avoid that.",
      "path": "pandas/core/construction.py",
      "line": 343,
      "original_line": 343,
      "position": 50,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()"
    },
    {
      "id": 3376046486,
      "user": "rhshadrach",
      "body": "Renamed to `result_has_mask`. Is that more clear?",
      "path": "pandas/tests/arrays/test_array.py",
      "line": null,
      "original_line": 614,
      "position": 1,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-3",
      "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in ["
    },
    {
      "id": 3386673182,
      "user": "jorisvandenbossche",
      "body": "Ah, I was assuming the constructed masked array would be returned directly, but there is indeed no `return`. Ignore!",
      "path": "pandas/core/construction.py",
      "line": 343,
      "original_line": 343,
      "position": 50,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()"
    },
    {
      "id": 3386679535,
      "user": "jorisvandenbossche",
      "body": "(but, that might not yet be explicitly covered by the tests?)",
      "path": "pandas/core/construction.py",
      "line": 343,
      "original_line": 343,
      "position": 50,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()"
    },
    {
      "id": 3386685683,
      "user": "jorisvandenbossche",
      "body": "Perfect!",
      "path": "pandas/tests/arrays/test_array.py",
      "line": null,
      "original_line": 614,
      "position": 1,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-3",
      "target_code": "@@ -577,37 +575,91 @@ def test_array_to_numpy_na():\n     tm.assert_numpy_array_equal(result, expected)\n \n \n-def test_pd_array_from_masked_array_preserves_mask_integer():\n+@pytest.mark.parametrize(\n+    \"data, mask\",\n+    [\n+        ([1, 2, 3], [False, True, False]),\n+        ([1, 2, 3], [False, False, False]),\n+        ([1, 2, 3], ma.nomask),\n+        ([], ma.nomask),\n+    ],\n+)\n+@pytest.mark.parametrize(\n+    \"np_dtype\",\n+    [\n+        \"uint8\",\n+        \"int32\",\n+        \"int64\",\n+        \"bool\",\n+        \"float16\",\n+        \"float32\",\n+        \"complex128\",\n+        \"datetime64[ns]\",\n+        \"timedelta64[ns]\",\n+    ],\n+)\n+@pytest.mark.parametrize(\"copy\", [True, False])\n+def test_masked_array_kinds(data, mask, np_dtype, copy):\n     # GH#63879\n-    # Integer masked array should produce Int64 with pd.NA where mask=True\n-    ma_arr = ma.array([1, 2, 3, 4], mask=[False, True, False, True], dtype=np.int64)\n-    result = pd.array(ma_arr)\n-    expected = pd.array([1, pd.NA, 3, pd.NA], dtype=\"Float64\")\n+    arr = np.array(data, dtype=np_dtype)\n+    ma_arr = ma.array(arr, mask=mask)\n+    result = pd.array(ma_arr, copy=copy)\n+    expected = pd.array(arr)\n+    if np.any(mask):\n+        na = np.nan if np_dtype in (\"float16\", \"complex128\") else pd.NA\n+        expected[np.asarray(mask)] = na\n     tm.assert_extension_array_equal(result, expected)\n \n+    # Check whether we made a copy.\n+    result_missing_mask = np_dtype in ["
    },
    {
      "id": 3399405684,
      "user": "rhshadrach",
      "body": "Yea - good call. Will add.",
      "path": "pandas/core/construction.py",
      "line": 343,
      "original_line": 343,
      "position": 50,
      "file_url": "https://github.com/pandas-dev/pandas/pull/65823/files#diff-2",
      "target_code": "@@ -327,14 +332,25 @@ def array(\n                 \"Cannot construct an array from an ndarray with compound dtype. \"\n                 \"Use DataFrame instead.\"\n             )\n-        elif data.mask is not np.False_ and ma.getmaskarray(data).any():\n-            na_dtype = ensure_dtype_can_hold_na(data.dtype)\n-            if na_dtype.char in \"SU\":\n-                na_dtype = np.dtype(\"object\")\n-            data = cast(\"ma.MaskedArray\", data.astype(na_dtype)).filled(np.nan)\n-        else:\n-            # No mask, convert to regular array\n+        mask = ma.getmask(data)\n+        if mask is ma.nomask:\n+            # Avoid materializing an all-False mask.\n             data = np.asarray(data)\n+        else:\n+            try:\n+                masked_cls = BaseMaskedDtype.from_numpy_dtype(\n+                    data.dtype\n+                ).construct_array_type()"
    },
    {
      "id": 4712558436,
      "user": "rhshadrach",
      "body": "@jorisvandenbossche - tests have been added.",
      "path": null,
      "line": null,
      "original_line": null,
      "position": null,
      "file_url": null,
      "target_code": "",
      "source": "issue_comment"
    },
    {
      "id": 4715818617,
      "user": "jorisvandenbossche",
      "body": "Thanks Richard!",
      "path": null,
      "line": null,
      "original_line": null,
      "position": null,
      "file_url": null,
      "target_code": "",
      "source": "issue_comment"
    }
  ]
}
